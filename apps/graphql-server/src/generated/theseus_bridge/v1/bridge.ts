// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.1
// source: theseus_bridge/v1/bridge.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CivicObject, TenantContext } from "../../civic_atlas/v1/civic_atlas.js";

export const protobufPackage = "theseus_bridge.v1";

export interface SpacetimeTopicRequest {
  tenantContext: TenantContext | undefined;
  topic: string;
  limit: number;
}

export interface SpacetimeTopicChunk {
  nodeId: string;
  title: string;
  summary: string;
  score: number;
  embedding: number[];
}

export interface SpacetimeEmbeddingRequest {
  tenantContext: TenantContext | undefined;
  nodeId: string;
}

export interface SpacetimeEmbeddingResponse {
  nodeId: string;
  embedding: number[];
  model: string;
}

export interface SearchObjectsRequest {
  tenantContext: TenantContext | undefined;
  query: string;
  limit: number;
}

export interface SearchObjectsResponse {
  objects: CivicObject[];
}

export interface IngestArtifactRequest {
  tenantContext: TenantContext | undefined;
  artifactId: string;
  sourceUri: string;
  payloadJson: string;
}

export interface IngestArtifactResponse {
  artifactId: string;
  status: string;
}

function createBaseSpacetimeTopicRequest(): SpacetimeTopicRequest {
  return { tenantContext: undefined, topic: "", limit: 0 };
}

export const SpacetimeTopicRequest: MessageFns<SpacetimeTopicRequest> = {
  encode(message: SpacetimeTopicRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantContext !== undefined) {
      TenantContext.encode(message.tenantContext, writer.uint32(10).fork()).join();
    }
    if (message.topic !== "") {
      writer.uint32(18).string(message.topic);
    }
    if (message.limit !== 0) {
      writer.uint32(24).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpacetimeTopicRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpacetimeTopicRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantContext = TenantContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.topic = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpacetimeTopicRequest {
    return {
      tenantContext: isSet(object.tenantContext)
        ? TenantContext.fromJSON(object.tenantContext)
        : isSet(object.tenant_context)
        ? TenantContext.fromJSON(object.tenant_context)
        : undefined,
      topic: isSet(object.topic) ? globalThis.String(object.topic) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: SpacetimeTopicRequest): unknown {
    const obj: any = {};
    if (message.tenantContext !== undefined) {
      obj.tenantContext = TenantContext.toJSON(message.tenantContext);
    }
    if (message.topic !== "") {
      obj.topic = message.topic;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<SpacetimeTopicRequest>): SpacetimeTopicRequest {
    return SpacetimeTopicRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpacetimeTopicRequest>): SpacetimeTopicRequest {
    const message = createBaseSpacetimeTopicRequest();
    message.tenantContext = (object.tenantContext !== undefined && object.tenantContext !== null)
      ? TenantContext.fromPartial(object.tenantContext)
      : undefined;
    message.topic = object.topic ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseSpacetimeTopicChunk(): SpacetimeTopicChunk {
  return { nodeId: "", title: "", summary: "", score: 0, embedding: [] };
}

export const SpacetimeTopicChunk: MessageFns<SpacetimeTopicChunk> = {
  encode(message: SpacetimeTopicChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    if (message.score !== 0) {
      writer.uint32(33).double(message.score);
    }
    writer.uint32(42).fork();
    for (const v of message.embedding) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpacetimeTopicChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpacetimeTopicChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.score = reader.double();
          continue;
        }
        case 5: {
          if (tag === 45) {
            message.embedding.push(reader.float());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.embedding.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpacetimeTopicChunk {
    return {
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      embedding: globalThis.Array.isArray(object?.embedding)
        ? object.embedding.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SpacetimeTopicChunk): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.score !== 0) {
      obj.score = message.score;
    }
    if (message.embedding?.length) {
      obj.embedding = message.embedding;
    }
    return obj;
  },

  create(base?: DeepPartial<SpacetimeTopicChunk>): SpacetimeTopicChunk {
    return SpacetimeTopicChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpacetimeTopicChunk>): SpacetimeTopicChunk {
    const message = createBaseSpacetimeTopicChunk();
    message.nodeId = object.nodeId ?? "";
    message.title = object.title ?? "";
    message.summary = object.summary ?? "";
    message.score = object.score ?? 0;
    message.embedding = object.embedding?.map((e) => e) || [];
    return message;
  },
};

function createBaseSpacetimeEmbeddingRequest(): SpacetimeEmbeddingRequest {
  return { tenantContext: undefined, nodeId: "" };
}

export const SpacetimeEmbeddingRequest: MessageFns<SpacetimeEmbeddingRequest> = {
  encode(message: SpacetimeEmbeddingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantContext !== undefined) {
      TenantContext.encode(message.tenantContext, writer.uint32(10).fork()).join();
    }
    if (message.nodeId !== "") {
      writer.uint32(18).string(message.nodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpacetimeEmbeddingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpacetimeEmbeddingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantContext = TenantContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpacetimeEmbeddingRequest {
    return {
      tenantContext: isSet(object.tenantContext)
        ? TenantContext.fromJSON(object.tenantContext)
        : isSet(object.tenant_context)
        ? TenantContext.fromJSON(object.tenant_context)
        : undefined,
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
    };
  },

  toJSON(message: SpacetimeEmbeddingRequest): unknown {
    const obj: any = {};
    if (message.tenantContext !== undefined) {
      obj.tenantContext = TenantContext.toJSON(message.tenantContext);
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    return obj;
  },

  create(base?: DeepPartial<SpacetimeEmbeddingRequest>): SpacetimeEmbeddingRequest {
    return SpacetimeEmbeddingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpacetimeEmbeddingRequest>): SpacetimeEmbeddingRequest {
    const message = createBaseSpacetimeEmbeddingRequest();
    message.tenantContext = (object.tenantContext !== undefined && object.tenantContext !== null)
      ? TenantContext.fromPartial(object.tenantContext)
      : undefined;
    message.nodeId = object.nodeId ?? "";
    return message;
  },
};

function createBaseSpacetimeEmbeddingResponse(): SpacetimeEmbeddingResponse {
  return { nodeId: "", embedding: [], model: "" };
}

export const SpacetimeEmbeddingResponse: MessageFns<SpacetimeEmbeddingResponse> = {
  encode(message: SpacetimeEmbeddingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    writer.uint32(18).fork();
    for (const v of message.embedding) {
      writer.float(v);
    }
    writer.join();
    if (message.model !== "") {
      writer.uint32(26).string(message.model);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpacetimeEmbeddingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpacetimeEmbeddingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 21) {
            message.embedding.push(reader.float());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.embedding.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.model = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpacetimeEmbeddingResponse {
    return {
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      embedding: globalThis.Array.isArray(object?.embedding)
        ? object.embedding.map((e: any) => globalThis.Number(e))
        : [],
      model: isSet(object.model) ? globalThis.String(object.model) : "",
    };
  },

  toJSON(message: SpacetimeEmbeddingResponse): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.embedding?.length) {
      obj.embedding = message.embedding;
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    return obj;
  },

  create(base?: DeepPartial<SpacetimeEmbeddingResponse>): SpacetimeEmbeddingResponse {
    return SpacetimeEmbeddingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpacetimeEmbeddingResponse>): SpacetimeEmbeddingResponse {
    const message = createBaseSpacetimeEmbeddingResponse();
    message.nodeId = object.nodeId ?? "";
    message.embedding = object.embedding?.map((e) => e) || [];
    message.model = object.model ?? "";
    return message;
  },
};

function createBaseSearchObjectsRequest(): SearchObjectsRequest {
  return { tenantContext: undefined, query: "", limit: 0 };
}

export const SearchObjectsRequest: MessageFns<SearchObjectsRequest> = {
  encode(message: SearchObjectsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantContext !== undefined) {
      TenantContext.encode(message.tenantContext, writer.uint32(10).fork()).join();
    }
    if (message.query !== "") {
      writer.uint32(18).string(message.query);
    }
    if (message.limit !== 0) {
      writer.uint32(24).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchObjectsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchObjectsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantContext = TenantContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchObjectsRequest {
    return {
      tenantContext: isSet(object.tenantContext)
        ? TenantContext.fromJSON(object.tenantContext)
        : isSet(object.tenant_context)
        ? TenantContext.fromJSON(object.tenant_context)
        : undefined,
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: SearchObjectsRequest): unknown {
    const obj: any = {};
    if (message.tenantContext !== undefined) {
      obj.tenantContext = TenantContext.toJSON(message.tenantContext);
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchObjectsRequest>): SearchObjectsRequest {
    return SearchObjectsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchObjectsRequest>): SearchObjectsRequest {
    const message = createBaseSearchObjectsRequest();
    message.tenantContext = (object.tenantContext !== undefined && object.tenantContext !== null)
      ? TenantContext.fromPartial(object.tenantContext)
      : undefined;
    message.query = object.query ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseSearchObjectsResponse(): SearchObjectsResponse {
  return { objects: [] };
}

export const SearchObjectsResponse: MessageFns<SearchObjectsResponse> = {
  encode(message: SearchObjectsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.objects) {
      CivicObject.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchObjectsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchObjectsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.objects.push(CivicObject.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchObjectsResponse {
    return {
      objects: globalThis.Array.isArray(object?.objects) ? object.objects.map((e: any) => CivicObject.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchObjectsResponse): unknown {
    const obj: any = {};
    if (message.objects?.length) {
      obj.objects = message.objects.map((e) => CivicObject.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SearchObjectsResponse>): SearchObjectsResponse {
    return SearchObjectsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchObjectsResponse>): SearchObjectsResponse {
    const message = createBaseSearchObjectsResponse();
    message.objects = object.objects?.map((e) => CivicObject.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIngestArtifactRequest(): IngestArtifactRequest {
  return { tenantContext: undefined, artifactId: "", sourceUri: "", payloadJson: "" };
}

export const IngestArtifactRequest: MessageFns<IngestArtifactRequest> = {
  encode(message: IngestArtifactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantContext !== undefined) {
      TenantContext.encode(message.tenantContext, writer.uint32(10).fork()).join();
    }
    if (message.artifactId !== "") {
      writer.uint32(18).string(message.artifactId);
    }
    if (message.sourceUri !== "") {
      writer.uint32(26).string(message.sourceUri);
    }
    if (message.payloadJson !== "") {
      writer.uint32(34).string(message.payloadJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestArtifactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestArtifactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantContext = TenantContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.artifactId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payloadJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestArtifactRequest {
    return {
      tenantContext: isSet(object.tenantContext)
        ? TenantContext.fromJSON(object.tenantContext)
        : isSet(object.tenant_context)
        ? TenantContext.fromJSON(object.tenant_context)
        : undefined,
      artifactId: isSet(object.artifactId)
        ? globalThis.String(object.artifactId)
        : isSet(object.artifact_id)
        ? globalThis.String(object.artifact_id)
        : "",
      sourceUri: isSet(object.sourceUri)
        ? globalThis.String(object.sourceUri)
        : isSet(object.source_uri)
        ? globalThis.String(object.source_uri)
        : "",
      payloadJson: isSet(object.payloadJson)
        ? globalThis.String(object.payloadJson)
        : isSet(object.payload_json)
        ? globalThis.String(object.payload_json)
        : "",
    };
  },

  toJSON(message: IngestArtifactRequest): unknown {
    const obj: any = {};
    if (message.tenantContext !== undefined) {
      obj.tenantContext = TenantContext.toJSON(message.tenantContext);
    }
    if (message.artifactId !== "") {
      obj.artifactId = message.artifactId;
    }
    if (message.sourceUri !== "") {
      obj.sourceUri = message.sourceUri;
    }
    if (message.payloadJson !== "") {
      obj.payloadJson = message.payloadJson;
    }
    return obj;
  },

  create(base?: DeepPartial<IngestArtifactRequest>): IngestArtifactRequest {
    return IngestArtifactRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IngestArtifactRequest>): IngestArtifactRequest {
    const message = createBaseIngestArtifactRequest();
    message.tenantContext = (object.tenantContext !== undefined && object.tenantContext !== null)
      ? TenantContext.fromPartial(object.tenantContext)
      : undefined;
    message.artifactId = object.artifactId ?? "";
    message.sourceUri = object.sourceUri ?? "";
    message.payloadJson = object.payloadJson ?? "";
    return message;
  },
};

function createBaseIngestArtifactResponse(): IngestArtifactResponse {
  return { artifactId: "", status: "" };
}

export const IngestArtifactResponse: MessageFns<IngestArtifactResponse> = {
  encode(message: IngestArtifactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.artifactId !== "") {
      writer.uint32(10).string(message.artifactId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestArtifactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestArtifactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.artifactId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestArtifactResponse {
    return {
      artifactId: isSet(object.artifactId)
        ? globalThis.String(object.artifactId)
        : isSet(object.artifact_id)
        ? globalThis.String(object.artifact_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: IngestArtifactResponse): unknown {
    const obj: any = {};
    if (message.artifactId !== "") {
      obj.artifactId = message.artifactId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<IngestArtifactResponse>): IngestArtifactResponse {
    return IngestArtifactResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IngestArtifactResponse>): IngestArtifactResponse {
    const message = createBaseIngestArtifactResponse();
    message.artifactId = object.artifactId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

export type TheseusBridgeDefinition = typeof TheseusBridgeDefinition;
export const TheseusBridgeDefinition = {
  name: "TheseusBridge",
  fullName: "theseus_bridge.v1.TheseusBridge",
  methods: {
    resolveSpacetimeTopic: {
      name: "ResolveSpacetimeTopic",
      requestType: SpacetimeTopicRequest as typeof SpacetimeTopicRequest,
      requestStream: false,
      responseType: SpacetimeTopicChunk as typeof SpacetimeTopicChunk,
      responseStream: true,
      options: {},
    },
    getSpacetimeEmbedding: {
      name: "GetSpacetimeEmbedding",
      requestType: SpacetimeEmbeddingRequest as typeof SpacetimeEmbeddingRequest,
      requestStream: false,
      responseType: SpacetimeEmbeddingResponse as typeof SpacetimeEmbeddingResponse,
      responseStream: false,
      options: {},
    },
    searchObjects: {
      name: "SearchObjects",
      requestType: SearchObjectsRequest as typeof SearchObjectsRequest,
      requestStream: false,
      responseType: SearchObjectsResponse as typeof SearchObjectsResponse,
      responseStream: false,
      options: {},
    },
    ingestArtifact: {
      name: "IngestArtifact",
      requestType: IngestArtifactRequest as typeof IngestArtifactRequest,
      requestStream: false,
      responseType: IngestArtifactResponse as typeof IngestArtifactResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
